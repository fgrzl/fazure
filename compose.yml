services:
  fazure:
    build:
      context: .
      dockerfile: cmd/Dockerfile
    container_name: fazure-emulator
    ports:
      - "10000:10000"
    environment:
      - PORT=10000
    # Uncomment the volumes section below to persist data to a local directory
    # volumes:
    #   - ./data:/data
    restart: unless-stopped
