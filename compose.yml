services:
  fazure:
    build:
      context: .
      dockerfile: cmd/Dockerfile
    container_name: fazure-emulator
    ports:
      - "10000:10000"
    environment:
      - DATA_DIR=/data
    volumes:
      - fazure-data:/data
    restart: unless-stopped
